name: Workflow 1

on:
    pull_request:
        types: [opened, reopened, synchronize]
        branches:
            - main

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
          - run: echo ${{ secrets.SECRET }}